{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Logs info-level messages for your app
      "Microsoft.AspNetCore": "Warning" // Logs warnings for ASP.NET Core framework
    }
  },
  "AllowedHosts": "*", // Allows any host to access the app (in production, you might want to specify hosts)
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=MunicipalityDB;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}
